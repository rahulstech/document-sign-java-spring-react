spring.application.name=docsign

# available profiles:
# dev
# dev-deploy
# prod
spring.profiles.active=dev

spring.datasource.url=jdbc:postgresql://${DB_URL}:${DB_PORT:5432}/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
